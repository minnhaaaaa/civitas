CIVITAS_POSTGRES_PORT=55432
DATABASE_URL=postgresql+psycopg://civitas:civitas_dev@localhost:55432/civitas
POSTGRES_DB=civitas
POSTGRES_USER=civitas
# Local demo only. Replace with a non-secret-manager-injected runtime value in production.
POSTGRES_PASSWORD=civitas_dev
CIVITAS_API_PORT=8000
CIVITAS_MCP_PORT=8001
# Set these only after the inbound-server and durable-worker packages are assembled.
# CIVITAS_MCP_SERVER_COMMAND=uvicorn civitas.mcp_server.http:create_app --factory --host 0.0.0.0 --port 8001
# CIVITAS_WORKER_COMMAND=python -m civitas.worker.main
# CIVITAS_SIMULATED_PROVIDER_COMMAND=python -m civitas.mcp_server.simulated_provider
